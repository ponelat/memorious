[workspace]
resolver = "2"
members = ["crates/core", "crates/mobile", "apps/server", "apps/desktop/src-tauri"]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"

[profile.dev]
# iroh + quinn are slow unoptimized; keep tests bearable
opt-level = 1
